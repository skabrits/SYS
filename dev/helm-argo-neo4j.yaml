apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: neoj4
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
  project: default
  source:
    path: neo4j
    repoURL: https://github.com/neo4j/helm-charts.git
    targetRevision: "5.24.1"
    helm:
      releaseName: arbitraz
      parameters:
      - name: "neo4j.name"
        value: "arbitraz"
      - name: "neo4j.password"
        value: "test-123"
      - name: "volumes.data.mode"
        value: "defaultStorageClass"
      - name: "env.NEO4J_PLUGINS"
        value: "[\"apoc\"]"
      - name: "env.NEO4J_apoc_export_file_enabled"
        value: "true"
        forceString: true
      - name: "env.NEO4J_apoc_import_file_enabled"
        value: "true"
        forceString: true
      - name: "env.NEO4J_apoc_import_file_use__neo4j__config"
        value: "true"
        forceString: true
      - name: "env.NEO4J_dbms_security_procedures_unrestricted"
        value: "apoc.*"
      
  destination:
    server: "https://kubernetes.default.svc"
    namespace: dev
